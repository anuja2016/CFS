
  // features details links
  $('#btn-frontoffice').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-frontoffice').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-fieldoperation').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-fieldoperation').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-customerexperience').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-customerexperience').offset().top - headerheight
    }, 'slow');
  });



  // features details links
  $('#btn-createquote').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-createquote').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-send-slatc').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-send-slatc').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-quoteapproval').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-quoteapproval').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-quotestatus').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-quotestatus').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-createjob').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-createjob').offset().top - headerheight
    }, 'slow');
  });


  // customer app links
  $('#btn-BrandCustomization').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-BrandCustomization').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-CustomerEngagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-CustomerEngagement').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ServiceBooking').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ServiceBooking').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-WebContent').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-WebContent').offset().top - headerheight
    }, 'slow');
  });

    // schedulling details links
  $('#btn-BookJob').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-BookJob').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-JobAssignment').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-JobAssignment').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-JobStatuses').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-JobStatuses').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-JobScheduling').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-JobScheduling').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-JobReminders').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-JobReminders').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-Map').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-Map').offset().top - headerheight
    }, 'slow');
  });

  // amc-management details links
  $('#btn-ContractDetails').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ContractDetails').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ServiceScheduling').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ServiceScheduling').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-RenewalReminders').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-RenewalReminders').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-BillingInvoicing').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-BillingInvoicing').offset().top - headerheight
    }, 'slow');
  });

   // asset-management details links
   $('#btn-ProducManagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ProducManagement').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ProductTracking').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ProductTracking').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-BarcodeScanning').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-BarcodeScanning').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-BreakdownHistory').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-BreakdownHistory').offset().top - headerheight
    }, 'slow');
  });

  // client-communication details links
  $('#btn-RealtimeUpdates').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-RealtimeUpdates').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-TwoWayCommunication').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-TwoWayCommunication').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-PersonalizedNotifications').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-PersonalizedNotifications').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-FeedbackLoops').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-FeedbackLoops').offset().top - headerheight
    }, 'slow');
  });

  // complete-job details links
  $('#btn-JobAlerts').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-JobAlerts').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-JobManagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-JobManagement').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-Navigation').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-Navigation').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-JobTracking').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-JobTracking').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-Expense').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-Expense').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-WorkAllocation').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-WorkAllocation').offset().top - headerheight
    }, 'slow');
  });

  // crm details links
  $('#btn-CustomerDatabaseManagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-CustomerDatabaseManagement').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ComplaintTracking').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ComplaintTracking').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-LeadManagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-LeadManagement').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-HistoricalInsights').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-HistoricalInsights').offset().top - headerheight
    }, 'slow');
  });


  // customer-app details links
  $('#btn-BrandingCustomization').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-BrandingCustomization').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-CustomerEngagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-CustomerEngagement').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ServiceBooking').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ServiceBooking').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-WebContent').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-WebContent').offset().top - headerheight
    }, 'slow');
  });

  // expense-management details links
  $('#btn-PettyCashManagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-PettyCashManagement').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ExpenseTracking').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ExpenseTracking').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ExpenseHistory').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ExpenseHistory').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-BalanceManagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-BalanceManagement').offset().top - headerheight
    }, 'slow');
  });


  // inventory-management details links
  $('#btn-AvailableQuantity').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-AvailableQuantity').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-AssignedItem').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-AssignedItem').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-UsedItems').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-UsedItems').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ReturnedItem').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ReturnedItem').offset().top - headerheight
    }, 'slow');
  });

  // invoice details links
  $('#btn-InvoiceQuickCreate').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-InvoiceQuickCreate').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-InvoiceStatuses').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-InvoiceStatuses').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ExportInvoicing').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ExportInvoicing').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ShareInvoice').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ShareInvoice').offset().top - headerheight
    }, 'slow');
  });

  // lead-management details links
  $('#btn-LeadCapture').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-LeadCapture').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-LeadAssignment').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-LeadAssignment').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-FollowUpReminders').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-FollowUpReminders').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-LeadStatuses').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-LeadStatuses').offset().top - headerheight
    }, 'slow');
  });

  // service-management details links
  $('#btn-CatalogManagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-CatalogManagement').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-CustomerAppIntegration').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-CustomerAppIntegration').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-ComplaintsForm').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-ComplaintsForm').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-RevenueGeneration').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-RevenueGeneration').offset().top - headerheight
    }, 'slow');
  });


  // technician-app details links
  $('#btn-RealTimeJobUpdates').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-RealTimeJobUpdates').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-Navigation').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-Navigation').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-JobChecklistsandForms').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-JobChecklistsandForms').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-PassbookManagement').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-PassbookManagement').offset().top - headerheight
    }, 'slow');
  });

  // technician-tracking details links
  $('#btn-LocationTracking').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-LocationTracking').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-GPSMonitoring').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-GPSMonitoring').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-DistanceTraveled').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-DistanceTraveled').offset().top - headerheight
    }, 'slow');
  });
  $('#btn-TaskStatus').click(function() {
    var headerheight = parseInt($(".fixed-top").height() + 20);
    $('html, body').animate({
      scrollTop: $('#tb-TaskStatus').offset().top - headerheight
    }, 'slow');
  });